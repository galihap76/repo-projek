unit latihan3;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  TForm1 = class(TForm)
    Label1: TLabel;
    usernameku: TEdit;
    passwordku: TEdit;
    Label2: TLabel;
    Label3: TLabel;
    Klik: TButton;
    procedure usernamekClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form1: TForm1;

implementation

{$R *.dfm}

procedure TForm1.usernamekClick(Sender: TObject);

begin
if (usernameku.Text = 'galih') and (passwordku.Text = '76') then
begin
 ShowMessage('Hai galih');
end
else
begin
    ShowMessage('Anda bukan galih!');
end;
end;

end.
